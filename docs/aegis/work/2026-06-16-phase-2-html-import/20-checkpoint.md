# Phase 2 HTML Import Checkpoint

## TodoCheckpointDraft

- [ ] Confirm Android SDK / Flutter device readiness.
- [x] Confirm Android SDK / Flutter device readiness.
- [x] Add Phase 2 plan and intent records.
- [x] Add parser tests and verify RED.
- [x] Implement parser and verify GREEN.
- [x] Add Isar import replacement API.
- [x] Add WebView import UI and timetable entry.
- [x] Generate Android runner and add WebView permission.
- [x] Build, test, analyze, and install to device.

## Active Slice

Completion verification.

## Completed

- ADB sees connected device `<device-id>` when run with required permissions.
- Android SDK installed at `<android-sdk>`.
- Flutter configured to use JDK 21 at `<jdk-21>`.
- Phase 2 parser, Isar replacement API, WebView import page, Android runner, and install flow are implemented.
- Debug APK installed to device `<device-id>`.

## ResumeStateHint

All planned Phase 2 initial import tasks are implemented. Next work should focus on school-specific HTML adapters and manual import UX validation against real教务系统 pages.

## DriftCheckDraft

Scope still matches Phase 2. No backend, sync, or widget work has been added.
